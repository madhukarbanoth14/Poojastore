-- No-op. Vendor confirm columns and the vendors table were already applied in
-- 20260904130000_order_vendor_confirm. This file remains so a previously failed
-- migration record can complete without altering schema twice.
SELECT 1;
